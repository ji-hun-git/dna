# Wave 4 evidence — 측정 이력 (parked), FHIR export, PR #5 F-3, Wave 3 minors (2026-09-17)

Branch `codex/wave8-measurement-history-fhir` (stacked on PR #11). Synthetic only. Release remains NO_GO; no readiness gate or verdict changed. Gate documents, committed before any code of this wave: `governance/intended-use-decision-measurement-history-2026-09-17.md` (item (c), "숫자 + 단순 그래프") and `governance/intended-use-decision-reference-range-and-delta-addendum-2026-09-17.md` (FHIR `referenceRange.text`).

## Scope note

The 측정 이력 screen (`/my-data/history`, plan Task 7) is **parked**. A mockup in the new visual language (dotted canvas, time-coloured ribbons, annotation cards, pixel numerals) was published for the founder at https://claude.ai/artifact/ApGNxjRga5x2jXg5dWASPq; the founder's answer was "나중에 볼게요, 백엔드 먼저" (later — backend first). No route, component, or CSS module for that screen exists on this branch: `pnpm --dir apps/web build`'s route list (below) has no `/my-data/history`, and Task 8's e2e work explicitly skipped every step that would navigate to, click toward, or assert on it (`apps/web/e2e/foundation-lifecycle.spec.ts`, marker comment "측정 이력 screen assertions are added with the screen task (parked pending mockup approval)."). The graph approved by gated item (c) of `governance/intended-use-decision-measurement-history-2026-09-17.md` is therefore not shown anywhere yet. The decision document itself is unaffected — it records what is permitted, not what is built.

## What exists now
- **Series API.** `GET /api/foundation/series` (owner-isolated, `Cache-Control: no-store`, CURRENT records only): one series per concept (same `concept_code`, else same label — connected components, because that rule is not transitive) and unit, points ordered by exam date (then confirmation instant, then record id). `derived` is always present and carries up to three numbers, always computed in time order: `lastDifference` (the Wave 3 calculator and its narrowed rules — percent only when the previous value is positive and the unit is not `%`), `per30Days` (HALF_EVEN, one more decimal than the inputs, omitted when the two dates are equal), `meanOfLast3` (HALF_EVEN, one more decimal, omitted under three numeric values); each key is simply omitted, never null, when not computable. No reference-range key or text, and no audit row is written for this read, consistent with the sibling reads.
- **측정 이력 (`/my-data/history`).** Not built. See "Scope note" above. `getSeries()` (`apps/web/lib/foundation/client.ts`) is a strict zod client for `/api/foundation/series` but is not yet called from any screen.
- **FHIR export.** `GET /api/foundation/health-events/export/fhir`: `application/fhir+json`, `Content-Disposition: attachment; filename="alm-health-events-<YYYYMMDD>.fhir.json"`, `Cache-Control: no-store`, `X-Content-Type-Options: nosniff`. A `collection` Bundle tagged `synthetic` (`meta.tag = [{system: "https://alm.example/fhir/tag", code: "synthetic"}]`); one hand-built `Observation` per CURRENT record, LOINC coding only when the concept table has one, `valueQuantity` (plain JSON number, never exponent form — `FhirQuantity.value`'s serializer was field-scoped after a review finding) or `valueString` for non-numeric values, `referenceRange[0].text` verbatim when the document printed a range (never low/high, never parsed), and `note` "본인이 값을 수정함" only when the value itself was corrected. No `interpretation`, `subject` or `performer` field exists on the DTO at all; no new dependency was added. Audit: `HEALTH_EVENTS_EXPORTED` with `resource_type = EXPORT_FHIR`, no value, count or date. The existing JSON export is unchanged. 데이터 관리 (`IntegratedDataControl.tsx`) has the second link "내 기록 내보내기(FHIR)", disabled at 0 events.
- **Wave 3 minors.** Two reference ranges on one row are kept verbatim joined by one space (`null` past 40 characters, `fbd1f4c`); the MedGemma side-by-side report now prints `referenceRangeAccuracy` (`1649a65`); the arrival class is cleared on `animationend` (`1649a65`).
- **PR #5 finding F-3** — fixed on PR #5's own branch (`codex/unified-health-product`, commit `7a4ef38`, PR comment https://github.com/ji-hun-git/dna/pull/5#issuecomment-5713218952): a bootstrap POST refused with 403/429 now keeps the person on the entry screen with its own sentence and next action, instead of falling into the generic "체험 상태를 불러오지 못했어요" restore-failed screen. That commit is **not** on this branch (`codex/wave8-measurement-history-fhir`); the fix was made in a separate temporary worktree against `codex/unified-health-product` and never touched this worktree.
- **Copy scan.** `apps/web/tests/korean-ux-copy.test.ts`'s forbidden list gains 빨라, 느려, 좋아, 나빠, 추세 (alongside the existing 상승, 하락, 증가, 감소) and runs over every user-facing file, including the two new components (`getSeries()` call sites and the FHIR export link).

## Evidence (local, 2026-09-17)
| Gate | Result |
|---|---|
| security:runtime-policy | `runtime-policy: PASS node=24.20.0 pnpm=11.20.0 next=16.3.3` |
| release:readiness:validate | exit 0; verdict line `release-readiness: NO_GO 12 blocking gate(s) are not PASS` (unchanged from Wave 3; no gate or verdict touched by this wave) |
| security:github-actions-policy | `github-actions-policy: PASS` |
| auth-security:gate | `auth-security-gate: PASS` |
| web:test | `Test Files  50 passed (50)` / `Tests  311 passed (311)` |
| tsc --noEmit | no output (exit 0) |
| apps/web build | `Compiled successfully`; route list: `/`, `/_not-found`, `/connections`, `/data-control`, `/healthz` (ƒ), `/my-data`, `/prepare`, `/providers`, `/records` — **no** `/my-data/history` (screen parked, see Scope note) |
| gradlew cleanTest test (embedded PostgreSQL) | `BUILD SUCCESSFUL in 1m 17s` (23 actionable tasks: 8 executed, 15 up-to-date); core-api 146 tests, 0 failures, 0 errors, 2 skipped; document-worker 35 tests, 0 failures, 0 errors, 1 skipped; document-boundary 13 tests, 0 failures, 0 errors, 0 skipped; korean-checkup-benchmark 5 tests, 0 failures, 0 errors, 0 skipped |
| medical-ai:native-text-gate | `"corpusId": "synthetic-ko-checkup-r2-e6befc286ae6ce1d"`, `"fieldF1": 1`, `"referenceRangeAccuracy": 1`, `"passed": true` |
| foundation:e2e | `3 passed (1.4m)` |
| readiness diff against the branch base (`af5e335030f8f51df5967094d02ed41bb7c66149`, merge-base with `origin/codex/wave7-reference-range-delta`) | `git diff --quiet af5e335 -- release/readiness.json` → exit 0 ("readiness unchanged") |

First commit of the branch (`git log --reverse "$mb"..HEAD \| head -1`): `4e363ca docs: Wave 4 intended-use decision (measurement history, three computed numbers, plain graph), FHIR range-text addendum, and design spec` — both decision documents precede all code of this wave.

`git status --short` after all gates and after restoring `apps/web/next-env.d.ts` shows only the source/doc files this wave touched (no `next-env.d.ts`, `test-results`, or `build/`).

## Limits
No hosted run. The three numbers are arithmetic on values the person confirmed; they carry no meaning. The 측정 이력 screen that would show the graph and the numbers together is not built — it is parked at the founder's request ("나중에 볼게요, 백엔드 먼저") pending review of the published mockup, so nothing on this branch yet displays a band, threshold, fitted line, forecast or direction language; adding the screen still needs no new intended-use decision (it is already covered by `governance/intended-use-decision-measurement-history-2026-09-17.md`) but does need a regulatory review before real personal health information is processed, exactly as that decision states. The FHIR file is a hand-written subset (no `Patient` resource, no UCUM coding, no `fullUrl`, not validated against any profile — no FHIR validator was run, only structural tests). FHIR import is out of scope. Known limits carried from review: a bare number printed before a range drops the range; a row with spaces inside two ranges abstains rather than being captured; `ratio(0,0) = 1` prints as 100% for a run with no matched measurements; owner isolation for the new reads is asserted with the same empty-result pattern used by the sibling reads, not a dedicated negative-authorization test; `lastDifference.percent`'s web-side schema also tolerates null. The founder's no-regulatory-review judgement (`governance/intended-use-decision-measurement-history-2026-09-17.md`) applies to synthetic staging only and must be revisited, with a regulatory review, before any real PHI is processed. Readiness is unchanged.
