# Roadmap — reviewed 2026-09-02

**Supersedes** the execution-status columns in `docs/superpowers/plans/README.md`. The six 2026-08-09 plans remain design references; this document is the sequencing authority until the next dated review.

**Principle:** run four tracks in parallel. Product value (A) must not wait for hosted staging (B); business validation (C) must not wait for either; the agent operating system (D) makes A and B cheaper. Every track ends in evidence, not intent.

## Track A — Product value (synthetic-only, no new authority)

| Step | Deliverable | Exit evidence | Status |
|---|---|---|---|
| A1 | Multi-candidate review: three ordered candidates per document, document completes after every decision | CI green with the updated PostgreSQL and browser lifecycle | in this wave |
| A2 | Records grouped by date and document; visit-preparation questions, printable | Vitest and Playwright coverage, copy scan extended | in this wave |
| A3 | Integrated UI becomes default; demo components retired | `GC_INTEGRATED_SYNTHETIC_UI` removed; copy scan covers all routes | next |
| A4 | Multiple synthetic document types (검진 결과지, 혈액검사, 처방전 요약) with distinct candidate sets keyed by digest | fixture manifest tested; timeline shows changes across two dates | next |
| A5 | Timeline "what changed" view with plain comparison of the user's own confirmed values, no judgement | copy review, axe, keyboard | after A4 |
| A6 | Public provider explorer on a rights-reviewed HIRA open-data snapshot (server-side, no key in browser) | connector record, licence, freshness, drift quarantine tests | needs public-data key (founder) |
| A7 | Deterministic explanation templates over confirmed facts (no model) | 100 percent hard-boundary eval pass | after A5 |

## Track B — Release ladder (hosted synthetic staging)

Unchanged from `PROJECT_GUIDE.md` section 9, with one correction:

| Step | Note |
|---|---|
| B0 | **GHCR root cause:** the publish workflow logs in with `github.token`, so packages inherit the public repository's visibility. Decide: private ECR (recommended), PAT-based private GHCR, or accept public. Then delete or leave the three packages accordingly. |
| B1 | Protect `main`: required PR, required `genome-companion-ci`, no force-push, linear history. |
| B2 | S3/SQS runtime adapters and hosted network boundary. |
| B3 | Account-backed OpenTofu plan with non-secret identifiers; separate apply approval. |
| B4 | Deploy synthetic staging with TLS, workload identity, managed secrets, observability, hosted denial probes. |
| B5 | Backup/restore, deletion replay, external audit anchor, research-runtime denial. |
| B6 | 400 percent zoom and screen-reader checks. |
| B7 | Re-evaluate `release/readiness.json`. |

## Track C — Business validation (no code)

| Step | Deliverable | Exit evidence |
|---|---|---|
| C1 | Five interviews per persona (checkup historian, cost-conscious planner, quantified-self) around an observed workflow | notes under `research/interviews/` with consent to record, no health data |
| C2 | Price test: one-time record report ₩19,000–39,000, membership ₩39,000–79,000, deposit-backed | conversion and refund evidence |
| C3 | Brand clearance for `앎` (KIPRIS search, domain, app-store handle) | clearance memo in `governance/` |
| C4 | Korean counsel memo on Medical Service Act Article 27 exposure for the fixed-fee model | memo reference (not the memo text) |
| C5 | MFDS applicability memo for the non-diagnostic record organiser | memo reference |

## Track D — Agent operating system

| Step | Deliverable | Status |
|---|---|---|
| D1 | `AGENTS.md`, skills (`gc-sanity-check`, `gc-safe-change`, `gc-korean-copy`, `gc-readiness-evidence`, `gc-synthetic-fixture`), agents (`gc-backend-engineer`, `gc-frontend-engineer`, `gc-design-reviewer`, `gc-safety-reviewer`, `gc-evidence-auditor`) | this wave |
| D2 | Local development note: pinned Node binary, PostgreSQL URL, what skips without Docker | next |
| D3 | Scheduled sanity check (weekly) that writes `docs/status/<date>/sanity-check.md` and opens a PR | after B1 |
| D4 | Skill evaluation harness: baseline-versus-skill scenarios rerun on skill edits | next |

## Founder-only actions this quarter

1. Decide B0 (registry) and execute B1 (branch protection). Both take under an hour.
2. Start C1 interviews; nothing technical blocks them.
3. Request the public-data API key for A6 and record it only in the secret manager.
4. Confirm whether the genetic-wallet track survives; if not, remove the mobile installers and their layout test.

## Decision rule for reordering

A step moves earlier only when its exit evidence is cheaper to obtain than the step ahead of it and it does not weaken a safety boundary. A step is removed when a dated review shows its premise no longer holds. Record each change in `governance/decision-log.md`.

## Appendix — skill check (Task 4)

Filled in by the implementation wave after the baseline-versus-skill runs. If this appendix is empty, the runs were not performed and the skills are unverified.
